<UserControl x:Class="MailKlient.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MailKlient.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300">
    <Button Background="Transparent" BorderThickness="1" HorizontalContentAlignment="Left">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image x:Name="contactImage" Grid.Column="0" Height="55"  Width="55"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10">
                <TextBlock x:Name="contactName" FontSize="14" FontFamily="Arial Unicode MS"/>
                <TextBlock x:Name="contactRole" FontFamily="Bahnschrift"/>
            </StackPanel>
        </Grid>
    </Button>
</UserControl>
